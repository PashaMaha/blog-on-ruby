<h1>All posts Milan</h1>
<% @post.each do |post| %>
  <div class="alert alert-light"></div>
  <h3> <%= post.title %></h3>
  <p> <%= post.body %></p>
  <%= link_to "more", post_path(post), :class => 'btn btn-outline-dark' %>
<% end %>
